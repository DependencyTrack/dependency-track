dt.config.profile=test

# Reduce to the lowest possible value to prevent
# excessive CPU usage during tests.
dt.bcrypt.rounds=4

# Use port 0 to let the OS assign an available port during tests.
dt.management.port=0
