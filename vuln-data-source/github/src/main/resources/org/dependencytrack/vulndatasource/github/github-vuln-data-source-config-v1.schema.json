{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dependencytrack.org/schemas/github-vuln-data-source-config-v1.schema.json",
  "type": "object",
  "javaInterfaces": [
    "org.dependencytrack.plugin.api.config.RuntimeConfig"
  ],
  "properties": {
    "enabled": {
      "type": "boolean",
      "title": "Enabled",
      "description": "Whether the GitHub Advisories data source should be enabled.",
      "existingJavaType": "boolean"
    },
    "aliasSyncEnabled": {
      "type": "boolean",
      "title": "Alias Synchronization Enabled",
      "description": "Whether to include alias information in vulnerability data."
    },
    "apiUrl": {
      "type": "string",
      "title": "GraphQL API URL",
      "description": "URL of GitHub's GraphQL API.",
      "format": "uri",
      "minLength": 1
    },
    "apiToken": {
      "type": "string",
      "title": "API Token",
      "description": "Personal Access Token to authenticate with the GitHub API.  \n**The token is only required for authentication and does not require any permissions**.  \nBoth [fine-grained](https://github.com/settings/personal-access-tokens/new) and [classic](https://github.com/settings/tokens/new) access tokens work.  \nMutually exclusive with the GitHub App credentials.",
      "minLength": 1,
      "x-secret-ref": true
    },
    "appId": {
      "type": "string",
      "title": "GitHub App ID",
      "description": "ID of the GitHub App to authenticate as. Configure together with the Installation ID and App Private Key as an alternative to the API Token.",
      "minLength": 1
    },
    "installationId": {
      "type": "string",
      "title": "GitHub App Installation ID",
      "description": "ID of the GitHub App installation to mint access tokens for.",
      "minLength": 1
    },
    "appPrivateKey": {
      "type": "string",
      "title": "GitHub App Private Key",
      "description": "PEM-encoded RSA private key of the GitHub App (PKCS#1, `-----BEGIN RSA PRIVATE KEY-----`), as downloaded from GitHub.",
      "minLength": 1,
      "x-secret-ref": true
    }
  },
  "required": [
    "enabled"
  ]
}